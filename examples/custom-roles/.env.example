# Copy this file to .env and fill in your own values.
# Never commit .env with a real secret.
#
# webgates_SHARED_SECRET must be a strong random string.
# Use a command like: openssl rand -hex 32
webgates_SHARED_SECRET=replace-with-a-strong-random-secret
